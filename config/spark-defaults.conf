# ============================================================
# Spark + Delta Lake - Configuração Padrão
# ============================================================

# Spark
spark.master                              local[*]
spark.app.name                            DataLakehouseMedallion
spark.sql.shuffle.partitions              4
spark.driver.memory                       2g
spark.executor.memory                     2g

# Delta Lake
spark.sql.extensions                      io.delta.sql.DeltaSparkSessionExtension
spark.sql.catalog.spark_catalog           org.apache.spark.sql.delta.catalog.DeltaCatalog

# Delta Lake - Configurações de Performance
spark.databricks.delta.schema.autoMerge.enabled        true
spark.databricks.delta.optimizeWrite.enabled           true
spark.databricks.delta.retentionDurationCheck.enabled  false

# MinIO / S3 (usado apenas com USE_MINIO=1; o warehouse é definido em src/session.py)
spark.hadoop.fs.s3a.endpoint                        http://localhost:9000
spark.hadoop.fs.s3a.access.key                     minioadmin
spark.hadoop.fs.s3a.secret.key                     minioadmin
spark.hadoop.fs.s3a.path.style.access              true
spark.hadoop.fs.s3a.impl                           org.apache.hadoop.fs.s3a.S3AFileSystem
spark.hadoop.fs.s3a.connection.ssl.enabled         false
spark.hadoop.fs.s3a.aws.credentials.provider       org.apache.hadoop.fs.s3a.SimpleAWSCredentialsProvider

# JARs (Delta + hadoop-aws) são adicionados por src/session.py via spark.jars.packages
# spark.jars.packages                           io.delta:delta-spark_2.12:3.2.0,org.apache.hadoop:hadoop-aws:3.3.4
# spark.sql.warehouse.dir é definido por src/session.py:
#   local -> ./spark-warehouse | USE_MINIO=1 -> s3a://lake/warehouse
